// import {DEventLog} from './eventlog'

// /**
//  * A remote event log is a proxy to an actual server based instance of an event log.
//  * It is only meant to synchronize events and thus only implements the EventLog
//  * interface and not the QueryableEventLog interface.
//  */
// class RemoteEventLog implements DEventLog {

// }
